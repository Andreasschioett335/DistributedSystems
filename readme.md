HOW TO RUN CHITCHAT:

1. Open a new terminal inside the project folder.
2. Run "cd server" followed by "go run server.go" and wait for the server to start up.
3. Open another terminal and run "cd client" followed by "go run client.go"
4. The client will now connect to the server.
5. Type into the client terminal and see the server respond.